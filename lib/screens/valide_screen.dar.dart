import 'package:flutter/material.dart';

class ValideScreen extends StatelessWidget {
  const ValideScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Écran Validé',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}